# Copy-Paste Brief

Use the text below in another AI chat to put it on roughly the same page fast.

```md
Read the docs in `/Users/abi/Documents/claude code/OP OSS` before doing anything:

1. `README.md`
2. `01-operating-rules.md`
3. `03-repo-work-ledger.md`
4. then the relevant project file

Important standing rules:

- Never commit directly to `main` or `master`
- Always use a non-default branch
- Always open a PR back to the default branch
- Never merge PRs directly
- Use the leak only as an architecture reference, never as donor code
- Do not work on `Open-Paws/open-paws-api-gateway` or `LarytheLord/AFA--open-paws--Resource-Chatbot` unless explicitly re-approved

Current strategic priorities:

- `Pocket Quant` for monetizable product work
- `Project Compassionate Code` for the strongest Codex hackathon pitch
- `Project Chimera` as the reusable agent / cognition framework

Important current repo state:

- `Open-Paws/open-paws-platform` PR `#42` merged
- `Open-Paws/project-compassionate-code` PR `#13` merged
- `Open-Paws/documentation` PR `#7` merged
- `LarytheLord/Project-Chimera` PR `#22` open
- `LarytheLord/Project-Chimera` PR `#23` open
- `LarytheLord/pocket-quant` PR `#1` open

Pocket Quant specifics:

- public repo: `https://github.com/LarytheLord/pocket-quant`
- active PR: `https://github.com/LarytheLord/pocket-quant/pull/1`
- local repo is still dirty beyond the PR-scoped runtime work
- do not assume all local files belong in PR `#1`
- best product position: India-first crypto portfolio intelligence layer, web-first, INR-native, read-only, risk/digest/alerts

Project Compassionate Code specifics:

- strongest Codex hackathon candidate
- pitch it as a Codex-powered open source contribution agent
- avoid leading with activism in the hackathon framing

Project Chimera specifics:

- Knight Medicare mainly uses Chimera as a memory + consciousness core behind a bridge
- Chimera should prioritize standalone parity, adapter seams, provenance, and policy before more research breadth
```

